{
  "name": "dokku-fastapi-react-monorepo",
  "description": "A FastAPI and React app for Dokku.",
  "scripts": {
    "heroku-postbuild": "npm install --prefix frontend && npm run build --prefix frontend && cp backend/requirements.txt ."
  },
  "cacheDirectories": ["frontend/node_modules"]
}